# macOS
.DS_Store

# Python virtual environment
.venv/
venv/

# Python bytecode/caches
__pycache__/
*.pyc

# C build artifacts (from `make run` / `make runq` / `make testcc` / `make win64`)
/run
/runq
/testc
*.exe
*.o

# Model checkpoints and exported weights (large, regenerated via train.py/export.py)
out/
*.pt
*.bin
!tokenizer.bin

# Datasets and pretokenized data (downloaded/generated by tinystories.py)
data/

# Downloaded test fixtures (fetched by test_all.py on first run)
test/

# Oracle activation dumps and comparison scratch output (oracle.py, compare.py)
dumps/

# wandb logging
wandb/
